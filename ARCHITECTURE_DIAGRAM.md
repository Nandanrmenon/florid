# Web Store Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         FLORID WEB STORE                             │
│                     (Flutter Web Application)                        │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ HTTP/Polling
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      PAIRING SERVICE                                 │
│               (In-Memory Message Queue)                              │
│                                                                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌────────────────────┐  │
│  │  Pairing Codes  │  │  Install Queue  │  │  Status Messages   │  │
│  │   (6-digit)     │  │  (Package Info) │  │  (Progress Data)   │  │
│  └─────────────────┘  └─────────────────┘  └────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                  ▲
                                  │ HTTP/Polling (5s)
                                  │
┌─────────────────────────────────────────────────────────────────────┐
│                      FLORID MOBILE APP                               │
│                   (Flutter Android Application)                      │
└─────────────────────────────────────────────────────────────────────┘
```

## Component Interaction Flow

### 1. Pairing Flow

```
┌─────────┐                    ┌──────────────┐                 ┌────────┐
│   Web   │                    │   Pairing    │                 │ Mobile │
│ Browser │                    │   Service    │                 │ Device │
└─────────┘                    └──────────────┘                 └────────┘
     │                                │                              │
     │                                │         Start Pairing        │
     │                                │◄─────────────────────────────│
     │                                │                              │
     │                                │    Generate 6-digit Code     │
     │                                │─────────────────────────────►│
     │                                │                              │
     │                                │         Display Code         │
     │                                │                        (123456)
     │                                │                              │
     │   Enter Code (123456)          │                              │
     ├───────────────────────────────►│                              │
     │                                │                              │
     │                                │      Validate & Pair         │
     │                                │◄─────────────────────────────│
     │                                │                              │
     │   Pairing Confirmed            │      Pairing Confirmed       │
     │◄───────────────────────────────│─────────────────────────────►│
     │                                │                              │
```

### 2. Install Request Flow

```
┌─────────┐                    ┌──────────────┐                 ┌────────┐
│   Web   │                    │   Message    │                 │ Mobile │
│ Browser │                    │    Queue     │                 │ Device │
└─────────┘                    └──────────────┘                 └────────┘
     │                                │                              │
     │  Click "Install" Button        │                              │
     ├───────────────────────────────►│                              │
     │                                │                              │
     │   Create Install Request       │                              │
     │   {packageName, appName}       │                              │
     ├───────────────────────────────►│                              │
     │                                │                              │
     │                                │        Poll for Requests     │
     │                                │◄─────────────────────────────│
     │                                │                              │
     │                                │      Return Install Request  │
     │                                │─────────────────────────────►│
     │                                │                              │
     │                                │      Show Notification       │
     │                                │                        📱 ding!
     │                                │                              │
     │                                │      User Taps Notification  │
     │                                │                        👆 tap
     │                                │                              │
     │                                │      Open Install Screen     │
     │                                │                         [📦]  │
     │                                │                              │
     │                                │      Download APK            │
     │                                │                      [▓▓▓░░]  │
     │                                │                              │
     │                                │    Send Status Updates       │
     │◄───────────────────────────────│◄─────────────────────────────│
     │                                │                              │
     │   Display "Installing..."      │                              │
     │                                │      Download Complete       │
     │◄───────────────────────────────│◄─────────────────────────────│
     │                                │                              │
     │                                │      User Installs APK       │
     │                                │                        [✓]    │
     │                                │                              │
```

### 3. Data Models

```
┌─────────────────────────────────────────────────────────────┐
│                     PairingMessage                           │
├─────────────────────────────────────────────────────────────┤
│  • type: MessageType (enum)                                 │
│    - pairRequest                                            │
│    - pairResponse                                           │
│    - installRequest                                         │
│    - installStatus                                          │
│    - heartbeat                                              │
│  • deviceId: String (unique device identifier)              │
│  • pairingCode: String (6-digit code)                       │
│  • data: Map<String, dynamic> (message payload)             │
│  • timestamp: DateTime (message creation time)              │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    InstallRequest Data                       │
├─────────────────────────────────────────────────────────────┤
│  • packageName: String (e.g., "org.fdroid.fdroid")          │
│  • appName: String (e.g., "F-Droid")                        │
│  • versionName: String? (optional version)                  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    InstallStatus Data                        │
├─────────────────────────────────────────────────────────────┤
│  • packageName: String                                       │
│  • status: String (downloading, complete, error)            │
│  • progress: double? (0.0 to 1.0)                           │
└─────────────────────────────────────────────────────────────┘
```

### 4. Screen Navigation

```
Mobile App Flow:
┌───────────────┐
│  FloridApp    │
│  (Main)       │
└───────┬───────┘
        │
        ├─► Home/Search/Updates (normal flow)
        │
        └─► Settings
                │
                └─► Pairing Screen
                        │
                        ├─► Display Code
                        └─► Manage Pairing
                                │
                                └─► (When install request arrives)
                                        │
                                        └─► Notification ────► Remote Install Screen
                                                                      │
                                                                      ├─► Download Progress
                                                                      ├─► Install Button
                                                                      └─► Success/Error

Web App Flow:
┌───────────────┐
│ Web Store     │
│ (Main)        │
└───────┬───────┘
        │
        ├─► Not Paired
        │     │
        │     └─► Pairing Dialog
        │              │
        │              └─► Enter Code ────► Paired!
        │
        └─► Paired
              │
              └─► Browse Apps
                    │
                    └─► Click Install ────► Request Sent
                                                  │
                                                  └─► Show Confirmation
```

### 5. Security Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                        Security Measures                          │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  1. Device ID Generation                                          │
│     └─► Random.secure() → 16 bytes → Base64 → 22 chars          │
│                                                                   │
│  2. Pairing Code                                                  │
│     └─► Random.secure() → 100000-999999 (6 digits)              │
│                                                                   │
│  3. Message Validation                                            │
│     └─► Requires: deviceId + pairingCode + timestamp            │
│                                                                   │
│  4. Message Expiry                                                │
│     └─► Messages older than 5 minutes are deleted               │
│                                                                   │
│  5. Pairing Validation                                            │
│     └─► Only paired devices can send install requests           │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

## Implementation Notes

### Current Implementation (In-Memory)

- ✅ Simple and fast
- ✅ No external dependencies
- ✅ Good for local testing
- ⚠️ Doesn't persist across sessions
- ⚠️ Devices must share same memory space

### Future Implementation (Server-Based)

```
┌──────────┐          ┌──────────┐          ┌──────────┐
│   Web    │          │  Server  │          │  Mobile  │
│ Browser  │◄────────►│   API    │◄────────►│  Device  │
└──────────┘          └──────────┘          └──────────┘
     │                      │                      │
     │    HTTPS/WSS         │      HTTPS/WSS       │
     │                      │                      │
     └──────────────────────┴──────────────────────┘
              Secure Communication Layer
```

Benefits:
- ✅ Works across networks
- ✅ Persistent storage
- ✅ Real-time updates (WebSocket)
- ✅ Better security (TLS)
- ✅ Multi-device support

## Platform Detection Logic

```dart
// In main.dart
home: !settings.isLoaded
    ? LoadingScreen()
    : kIsWeb                          // ← Platform detection
    ? WebStoreScreen()                // Web platform
    : settings.onboardingComplete     // Mobile platform
    ? FloridApp()
    : OnboardingScreen()
```

## Key Files Reference

```
lib/
├── main.dart                         # Entry point, platform detection
├── services/
│   └── pairing_service.dart          # Core pairing logic (460 lines)
├── providers/
│   └── pairing_provider.dart         # State management (145 lines)
├── screens/
│   ├── pairing_screen.dart           # Mobile pairing UI (265 lines)
│   ├── remote_install_screen.dart    # Install progress (350 lines)
│   ├── web_store_screen.dart         # Web store UI (440 lines)
│   ├── florid_app.dart               # Main app, polling added
│   └── settings_screen.dart          # Added pairing menu item
└── test/
    └── pairing_service_test.dart     # Unit tests (72 lines)
```

Total new code: ~1,700 lines
